export const userTypes = ["buyer", "seller"];
/** Used for testing only */
export const currentTime = new Date("2022-01-01");
/** Used for testing only */
export const mockPwd = "hashedPwd";
/** Used for testing only */
export const mockToken = "generatedToken";
